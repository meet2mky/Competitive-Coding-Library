#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10; // limit of sieve
vector<bool> prime(N,true);
void sieve(){
    prime[0]=prime[1]=false;
    int lim = sqrt((double)N);
    for(int i=2;i<=lim;i++){
        if(prime[i]){
          for(int j= 2*i; j<N;j+=i)prime[j]=false;  
        }
    }
}
int main(){
  // read ..... n.....
  sieve();
  cout << prime[10] << endl;
  cout << prime[23] << endl;
  return 0;
 }
